log.script("cmr.script")

go.property("_cls", hash(""))
go.property("_name", hash(""))

function init(_s)
	Cmr.init(_s)
end

function update(_s, dt)
	_s:upd(dt)
end

function on_message(_s, msg_id, prm, sender)
	_s:on_msg(msg_id, prm, sender)
end

function final(_s)
	_s:final()
end

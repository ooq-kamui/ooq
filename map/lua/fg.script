log.scrpt("fg.script")

go.property("_nameHa", hash(""))
go.property("_areaHa", hash(""))
go.property("_scrl_rate", 1)

function init(_s)
	Fg.init(_s)
end

---[[ upd
function update(_s, dt)
	_s:upd(dt)
end
--]]

function on_message(_s, msg_id, prm, sndr_url)
	_s:on_msg(msg_id, prm, sndr_url)
end

